<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.map.food.mapper.apiData.RestaurantIDXMapper">

    <select id="countByResIdx" parameterType="String" resultType="Integer">
      SELECT COUNT(*) FROM restaurant WHERE RESIDX = #{RESIDX}
    </select>

</mapper>